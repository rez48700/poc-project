class instruction {
    constructor(type, input) {
        this.type = type; 
        this.input = input; 
    }
}

class variable {
    constructor(name, value) {
        this.name = name; 
        this.value = value; 
    }
}